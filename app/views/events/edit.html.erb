<h1 class="notification">Edit <span class="orange"><%=@event.name%></span></h1>
<%= form_for @event do |f| %>
<div class="form">
  <table>
    <tr>
      <th>Type</th>
      <th>Value</th>
    </tr>
    <tr>
      <td style="width: 150px;">Name</td>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <td style="width: 150px;">Date</td>
      <td><%= f.text_field :date %></td>
    </tr>
    <tr>
      <td style="width: 150px;">Type</td>
      <td>
        <%=f.select :event_type, options_for_select(['Netrunner','Magic The Gathering','Pokemon'])%>
      </td>
    </tr>
    <tr>
      <td style="width: 150px;">Bracket</td>
      <td>
        <%=f.select :bracket, options_for_select(%w[Non-Elimination])%>
      </td>
    </tr>
    <tr style="height: 150px">
      <td style="width: 150px;">Description</td>
      <td><%=f.text_area :description%></td>
    </tr>
  </table>
<%= f.submit 'Update Event', :class => "submit"%>
<% end%>
<div style="white-space:nowrap;">
  <a href=<%=event_path(@event)%>href=<%=event_path(@event)%> data-confirm="Are you sure you want to delete this event?" data-method="delete"><button class="submit" style="background: #db4435; margin-left: 15px;">Delete</button></a>
  <a href=<%=event_path(@event)%>><button class="submit" style="background: #b3b3b3; margin-left: 15px;">Cancel</button></a>
  </div>
</div>
